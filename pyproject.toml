[project]
name = "booking-planner-for-testlab"
version = "2.1.0"
description = "Webová aplikace pro správu rezervací testovací laboratoře"
requires-python = ">=3.10"
dependencies = [
    "flask==3.1.2",
    "requests==2.32.5",
]

[tool.uv]
package = false
